import { useContext } from "react";
import DisplayContentContext from "../Context/DisplayContent";

const UseDisplayContent = () => {
  return useContext(DisplayContentContext);
};

export default UseDisplayContent;
